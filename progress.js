document.addEventListener('DOMContentLoaded',()=>{const lessons=JSON.parse(localStorage.getItem('lessons')||'[]');const best=Number(localStorage.getItem('bestScore')||0);const streak=Number(localStorage.getItem('streak')||0);document.getElementById('lessonCount').textContent=lessons.length;document.getElementById('bestScore').textContent=best+'%';document.getElementById('streak').textContent=streak;document.getElementById('lessonList').innerHTML=lessons.map(l=>`<li>${l}</li>`).join('')||'<li>No completed lessons yet.</li>';let overall=Math.min(100,Math.round((lessons.length/4)*60 + best*0.4));document.getElementById('overallBar').style.width=overall+'%';showLeaderboard();});
function showLeaderboard(){const board=JSON.parse(localStorage.getItem('leaderboard')||'[]');document.getElementById('leaderboard').innerHTML=board.map((b,i)=>`<p>${i+1}. ${b.name} - ${b.score}% (${b.date})</p>`).join('')||'<p>No quiz scores yet.</p>';}
function downloadCertificate(){const p=JSON.parse(localStorage.getItem('profile')||'{"name":"Learner"}');const best=localStorage.getItem('bestScore')||0;const text=`Certificate of Completion\n\nThis certificate is proudly presented to ${p.name||'Learner'} for participating in Smart Learning Hub.\nBest Quiz Score: ${best}%\nDate: ${new Date().toLocaleDateString()}`;const blob=new Blob([text],{type:'text/plain'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='Smart-Learning-Hub-Certificate.txt';a.click();}
